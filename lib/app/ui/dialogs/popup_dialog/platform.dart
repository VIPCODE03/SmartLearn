///---------------class Platform---------------
/// Lớp giả lập Platform để kiểm tra xem ứng dụng đang chạy trên iOS hay không.
class Platform {
  ///---------------static const bool isIOS---------------
  /// Hằng số boolean cho biết ứng dụng có đang chạy trên iOS hay không. Trong trường hợp này, nó luôn trả về false.
  static const bool isIOS = false;
}